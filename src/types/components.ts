export interface HeaderLink {
  label: string;
  link?: string;
  isSelected?: boolean;
  desc?: string;
  icon?: JSX.Element;
}

export interface DDMItem {
  icon?: JSX.Element;
  label: string;
  desc?: string;
  link?: string;
}
